<div class = "jumbotron homebanner">
  <h2>Gym Rank is the best way to find reviews on local gyms.</h2>
  <h3>Explore the best gyms, pools, and sport courts in your area.<br> Leave reviews and ratings for your fellow fitness freaks!</h3>
</div>


<table class="table table-hover table-responsive">
  <thead>
    <tr>
      <th>Name</th>
      <th>Address</th>
      <th>Phone</th>
      <th>Website</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @gyms.each do |gym| %>
      <tr>
        <td><%= gym.name %></td>
        <td><%= gym.address %></td>
        <td><%= gym.phone %></td>
        <td><%= gym.website %></td>
        <td><%= link_to 'Show', gym, class: "btn btn-link" %></td>
        <td><%= link_to 'Edit', edit_gym_path(gym), class: "btn btn-link" %></td>
        <td><%= link_to 'Destroy', gym, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-link" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Gym', new_gym_path, class: "btn btn-link" %> <br>

<div class = "gimpic">
  <%= image_tag "https://s3-us-west-2.amazonaws.com/gymrank/grGimpLogo.png" %>
</div>
